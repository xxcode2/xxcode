{
  "name": "xxcode",
  "version": "1.0.0",
  "description": "xxcode Portfolio Website",
  "main": "script.js",
  "scripts": {
    "dev": "tailwindcss -i ./input.css -o ./public/styles.css --watch",
    "build": "tailwindcss -i ./input.css -o ./public/styles.css --minify && mkdir -p public && cp -r index.html script.js public/images public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.0"
  }
}
